# Smart-Diff CI

A lightweight, language-agnostic CLI tool for Test Impact Analysis (TIA) that runs only affected tests in CI and local environments.

## Overview
Smart-Diff analyzes Git diffs and codebase dependency graphs to identify exactly which tests need to run, significantly reducing CI duration and costs.

## Getting Started
(Documentation coming soon)
